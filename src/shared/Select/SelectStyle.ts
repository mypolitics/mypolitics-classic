import styled from 'styled-components';

export const Container = styled.select`
  padding: 1rem;
  margin: 0;
  border: 0;
  border-radius: 0.5rem;
  background: #00b4db;
  color: var(--white);
  font-weight: 600;
`;
